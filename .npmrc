; Public-repo override: route @cloudflare/* through public npm so bun.lock
; doesn't leak the internal Cloudflare gateway URL into the public lockfile.
@cloudflare:registry=https://registry.npmjs.org/
